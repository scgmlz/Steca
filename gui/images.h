#ifndef IMAGES_H
#define IMAGES_H

/*#include "pane.h"
#include <QListWidget>

class ImageList: public QListWidget {
  SUPER(ImageList,QListWidget)
public:
  ImageList();
};

class Images: public Pane {
  SUPER(Images,Pane)
public:
  Images();

private:
  ImageList *l;
};*/

#endif
